TODO LIST


Dans le main :

    - Charger le dictionnaire -> Lire -> Mettre dans un vercteur
    - Charger le livre -> Lire ->  Traiter chaque ligne le texte du livre -> Mettre dans un vercteur
    - Pour chaque ligne du livre :
        - On découpe en mots
        - On met les mot dans un vecteur "ligne"
        - On push le vecteur dans le vecteur "livre"

    - Ordrer le dictionnaire
    - Pour chaque mot du livre :
        - Recherche "dicotomique ou pas" du mot dans le dictionnaire
            - Si mot non trouvé :
                - Mettre le mot + num de ligne dans un vecteur<string>



Fonctions :

vector<string> separerMots(const string& ligne);

vector<string> lireTexte(const string& fichier);

normaliserMot(); //A améliorer

ordrerDictionnaire();

fonction traitement du texte, pour enlever les ". , - &" , nombre etc...
